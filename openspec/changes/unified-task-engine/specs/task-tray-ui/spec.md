## ADDED Requirements

### Requirement: 前端任务托盘交互
前端必须提供非阻断的任务中心界面，展示活跃任务进度。

#### Scenario: 任务中心全局感知
- **WHEN** 后台有任务运行时
- **THEN** 界面右上角显示任务托盘图标及活跃任务数量统计

#### Scenario: 非阻断导航切换
- **WHEN** 用户在“添加机器人”任务运行期间切换到“在线聊天”
- **THEN** 任务继续在后台执行，且托盘中的进度条持续更新
